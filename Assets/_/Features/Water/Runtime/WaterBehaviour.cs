using System;
using CustomTools;
using UnityEngine;

namespace Water.Runtime
{
    public class WaterBehaviour : Tools
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
            /*
            Info("C'est la mer noir.");
        */
        }
        
    }
}
